type SxProps = {
  padding: string;
  color: string;
  fontSize: string;
  borderSize: string;
  borderStyle: string;
  borderColor: string;
  borderRadius: string;
};

export const style: SxProps = {
  padding: '0.75rem',
  color: 'primary.main',
  fontSize: '1rem',
  borderSize: '1px',
  borderStyle: 'solid',
  borderColor: 'surface.main',
  borderRadius: '0.5rem',
};
